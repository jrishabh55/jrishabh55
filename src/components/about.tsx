import { ABOUT } from "@/config";

const About = () => (
  <section id="about" className="backdrop-blur-sm">
    <h2 className="mb-4 text-3xl font-bold">About</h2>
    <p className="mb-4 text-gray-400">{ABOUT}</p>
  </section>
);

export default About;
